## 1. UI 准备

- [x] 1.1 在历史记录面板中添加搜索输入框 HTML 元素
- [x] 1.2 添加搜索输入框的 CSS 样式（隐藏状态、聚焦状态、搜索模式样式）
- [ ] 1.3 添加搜索图标或提示文字（可选）

## 2. 状态管理

- [x] 2.1 定义搜索模式状态变量（searchMode, searchQuery）
- [x] 2.2 实现进入搜索模式函数（enterSearchMode）
- [x] 2.3 实现退出搜索模式函数（exitSearchMode）
- [x] 2.4 实现清空搜索状态函数（clearSearch）

## 3. 键盘事件处理

- [x] 3.1 在全局 keydown 事件中添加 `/` 键拦截逻辑（非搜索模式下触发）
- [x] 3.2 在全局 keydown 事件中添加 ESC 键处理（搜索模式下退出）
- [x] 3.3 搜索模式下阻止 `/` 键的默认行为（作为字符输入）
- [x] 3.4 搜索模式下确保其他按键正常工作（不干扰输入）

## 4. 搜索输入框交互

- [x] 4.1 实现搜索框的 input 事件监听（触发过滤）
- [x] 4.2 实现搜索框的 focus/blur 事件处理（状态同步）
- [x] 4.3 实现搜索框清空功能（backspace、delete、清空按钮）

## 5. 过滤逻辑

- [x] 5.1 实现历史记录过滤函数（基于 content 字段，不区分大小写）
- [x] 5.2 使用 requestAnimationFrame 优化过滤性能
- [x] 5.3 实现过滤结果的 DOM 更新（显示/隐藏记录项）
- [x] 5.4 实现清空搜索时恢复所有记录显示

## 6. 集成和状态同步

- [x] 6.1 将搜索功能集成到历史记录面板的初始化流程
- [x] 6.2 确保搜索模式不影响原有功能（复制、删除、双击复制）
- [x] 6.3 实现搜索模式下的焦点管理（确保输入框保持焦点）

## 7. 测试和验证

- [x] 7.1 测试 `/` 键进入搜索模式
- [x] 7.2 测试 ESC 键退出搜索模式
- [x] 7.3 测试实时过滤功能（输入字符即时匹配）
- [x] 7.4 测试搜索模式中 `/` 作为字符输入
- [x] 7.5 测试过滤过程不中断用户输入（异步执行）
- [x] 7.6 测试原有功能不受影响（复制、删除）